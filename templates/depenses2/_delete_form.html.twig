<form method="post" action="{{ path('app_depenses_delete', {'id': depense.id_depense}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depense.id_depense) }}">
    <button class="btn">Delete</button>
</form>
