<title>Edit Pret</title>

{% block body %}
	<h1>Edit Pret</h1>

	{{ include('pret/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_pret_list') }}">back to list</a>

	{{ include('pret/_delete_form.html.twig') }}
{% endblock %}
